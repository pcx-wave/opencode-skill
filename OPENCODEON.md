---
name: opencodeon
description: Enable OpenCode auto-delegate mode — all coding tasks are automatically sent to OpenCode CLI without requiring /opencode each time.
license: MIT
user-invocable: true
allowed-tools:
  - bash
---

# /opencodeon

Run: `touch ~/.local/share/opencode-auto.flag`

Then confirm: "Auto-opencode ON — every coding request will be delegated to OpenCode automatically."
